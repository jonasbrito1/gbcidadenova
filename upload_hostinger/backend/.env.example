# ================================================================
# CONFIGURAÇÃO DO BACKEND - HOSTINGER
# ================================================================
#
# INSTRUÇÕES:
# 1. Renomeie este arquivo para: .env
# 2. Preencha TODAS as variáveis abaixo
# 3. NUNCA compartilhe este arquivo com senhas reais
# ================================================================

# Ambiente
NODE_ENV=production

# Porta do servidor (Hostinger usa porta 3000 automaticamente)
PORT=3000

# ================================================================
# BANCO DE DADOS HOSTINGER
# ================================================================
DB_HOST=127.0.0.1
DB_PORT=3306
DB_USER=u674882802_jonasgb
DB_PASSWORD=COLOCAR_SENHA_DO_BANCO_AQUI
DB_NAME=u674882802_gb

# ================================================================
# SEGURANÇA - GERAR NOVOS SECRETS
# ================================================================
# Para gerar JWT_SECRET execute no Node.js:
# node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
JWT_SECRET=SUBSTITUIR_POR_SECRET_GERADO_AQUI

# Para gerar ENCRYPTION_KEY execute:
# node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
ENCRYPTION_KEY=SUBSTITUIR_POR_KEY_GERADA_AQUI

# Para gerar SESSION_SECRET execute:
# node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
SESSION_SECRET=SUBSTITUIR_POR_SESSION_SECRET_AQUI

# ================================================================
# CORS - SUBSTITUIR PELO SEU DOMÍNIO
# ================================================================
ALLOWED_ORIGINS=https://seudominio.com.br,https://www.seudominio.com.br

# ================================================================
# RATE LIMITING
# ================================================================
RATE_LIMIT_WINDOW=15
RATE_LIMIT_MAX=100

# ================================================================
# GOOGLE OAUTH (Opcional - deixe vazio se não usar)
# ================================================================
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
GOOGLE_CALLBACK_URL=https://seudominio.com.br/api/auth/google/callback

# ================================================================
# EMAIL / SMTP (Opcional - para notificações)
# ================================================================
SMTP_HOST=smtp.hostinger.com
SMTP_PORT=465
SMTP_SECURE=true
SMTP_USER=seu-email@seudominio.com.br
SMTP_PASS=SENHA_DO_EMAIL_AQUI
EMAIL_FROM=seu-email@seudominio.com.br

# ================================================================
# UPLOADS
# ================================================================
MAX_FILE_SIZE=10485760
UPLOAD_PATH=./uploads

# ================================================================
# LOGS
# ================================================================
LOG_LEVEL=info
LOG_FILE=./logs/production.log
